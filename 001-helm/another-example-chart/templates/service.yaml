apiVersion: v1
kind: Service
metadata:
  name: {{ include "another-example-chart.fullname" . }}
  labels:
    {{- include "another-example-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "another-example-chart.selectorLabels" . | nindent 4 }}
